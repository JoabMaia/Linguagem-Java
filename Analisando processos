import java.util.*;

public class TreinoAula{
	public static void main(String[] args){
		Scanner in = new Scanner(System.in);
		int minuto, resultado;
		
		minuto = Integer.parseInt(in.nextLine());
		
		resultado = (60/minuto) * 8;
		
		System.out.printf("%d", resultado);		
}
}
